# 11489 Integer Game

輸入一個整數，判斷每次移除一個位數是否還能讓這個數字被三整除，用除3的特性，把所有位數加起來的值能被3整除就可以。
用字串存該數，並用一個陣列分類該數字的每個位數除3後餘0, 1, 2，然後一開始輸入的數字有不同的條件要處理。

如果一開始的數不能被3整除，看餘1還餘2，並檢查是否有餘1或餘2的數能移除掉，可以就讓玩家S拿走該餘1或餘2的數。
並增加一個回合。因為每回合都要數字能被3整除，只能移除0, 3, 6, 9這四個數字，所以只要再去看有幾個整除的數就是總回合。

看回合數是奇數還偶數，就知道誰贏了。